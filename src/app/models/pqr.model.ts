import { User } from './user.model';

export class Pqr {
    id?: number;
    description?: string;
    answer?: string;
    createdDate?: Date;
    answered?: boolean;
    userId?: number;
    user?: User;
}

